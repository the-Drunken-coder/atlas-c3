# Component Contracts

This folder will define shared component contracts used inside entity and task JSON.

Component contracts should define:

- component name
- owning record family
- allowed shape
- validation boundaries
- applicability by entity or task type
- update semantics

Until individual component contracts are written, docs may reference this overview as the canonical location for future component definitions.

